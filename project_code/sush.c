/**
 * @file sush.c
 * @author Isabella Boone 
 * @author John Gable 
 * @author Hannah Moats
 * @brief 
 * @version 0.1
 * @date 2021-03-16
 */

#include <stdio.h> 
#include "Commands/file.h"
#include "Commands/commandlist.h"

/**
 * @brief Project 2: Shell Project 
 * 
 * @return int 
 */
int main(void) {
  /*  State Machine

  start

  while(parsing || no errors) {
    int whitespace_pos = -1; 
    int digit_pos = -1; 

    if(whitespace) { // whitespace = spaces & tabs
      // keep eating until digit 
      // record position of this character as start
      whitespace_pos = 
    } else if (digit) { 
      // keep eating until whitespace
      // use position at end of number 
      // strndup from starting position

    }

  }

  */
}

